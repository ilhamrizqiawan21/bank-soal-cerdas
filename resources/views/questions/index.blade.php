@extends('layouts.app')
@section('title','Bank Soal')
@section('breadcrumb','Bank Soal')
@section('content')
<div class="container-fluid">
<div class="d-flex justify-content-between align-items-center mb-4"><h5 class="fw-bold mb-0">Daftar Soal</h5><div class="d-flex gap-2"><a href="{{ route('subjects.index') }}" class="btn btn-outline-primary btn-sm"><i class="fas fa-book me-1"></i> Mata Pelajaran</a><button class="btn btn-success btn-sm" data-bs-toggle="modal" data-bs-target="#importModal"><i class="fas fa-file-import me-1"></i> Import</button><a href="{{ route('questions.export') }}" class="btn btn-info btn-sm"><i class="fas fa-file-export me-1"></i> Export</a><a href="{{ route('questions.create') }}" class="btn btn-primary btn-sm"><i class="fas fa-plus me-1"></i> Tambah Soal</a></div></div>
<div class="stat-card mb-4" x-data="questionFilter()"><form method="GET" action="{{ route('questions.index') }}"><div class="row g-3 align-items-end"><div class="col-md-2"><label class="form-label small fw-bold">Kurikulum</label><select name="curriculum" x-model="curriculum" class="form-select form-select-sm"><option value="semua">Semua</option><option value="merdeka">Merdeka</option><option value="kbc">KBC</option></select></div><div class="col-md-2"><label class="form-label small fw-bold">Level Kognitif</label><select name="level_c" x-model="level" class="form-select form-select-sm"><option value="semua">Semua</option><option value="L1">L1 - Pengetahuan & Pemahaman (C1-C2)</option><option value="L2">L2 - Penerapan (C3)</option><option value="L3">L3 - Penalaran / HOTS (C4-C6)</option></select></div><div class="col-md-2"><label class="form-label small fw-bold">Tipe Soal</label><select name="type" x-model="type" class="form-select form-select-sm"><option value="semua">Semua</option><option value="pg">PG</option><option value="uraian">Uraian</option><option value="menjodohkan">Menjodohkan</option><option value="benar_salah">Benar/Salah</option></select></div><div class="col-md-2"><label class="form-label small fw-bold">KKO</label><select name="kko_id" x-model="kko" class="form-select form-select-sm"><option value="semua">Semua</option>@foreach($kkoList as $kko)<option value="{{ $kko->id }}">{{ $kko->verb }} ({{ $kko->bloom_level ?? 'C?' }} · {{ $kko->level }})</option>@endforeach</select></div><div class="col-md-2"><label class="form-label small fw-bold">Cari</label><input name="search" x-model="search" class="form-control form-control-sm" placeholder="Cari soal..."></div><div class="col-md-2"><button class="btn btn-primary btn-sm w-100"><i class="fas fa-search me-1"></i> Filter</button></div></div></form></div>
<div class="table-responsive"><table class="table table-striped table-hover table-soal mb-0"><thead><tr><th>No</th><th>Soal</th><th>Tipe</th><th>Kurikulum</th><th>Level</th><th>KKO</th><th>Indikator</th><th>Aksi</th></tr></thead><tbody>@forelse($questions as $index=>$question)<tr><td>{{ $questions->firstItem()+$index }}</td><td><div class="soal-text">{{ Str::limit($question->question_text,100) }}</div><small class="text-muted">{{ $question->subject->name ?? '-' }} · {{ $question->jenjang }}</small></td><td><span class="badge bg-secondary">{{ $question->type_label }}</span></td><td><span class="badge {{ $question->curriculum === 'merdeka' ? 'badge-merdeka':'badge-kbc' }}">{{ $question->curriculum_label }}</span></td><td><span class="badge badge-{{ strtolower($question->level_c) }}">{{ $question->level_c }}</span><span class="badge {{ $question->hots_level==='HOTS' ? 'bg-danger':($question->hots_level==='MOTS'?'bg-warning':'bg-info') }} text-white ms-1">{{ $question->hots_level }}</span></td><td>{{ $question->kko->verb ?? '-' }} <small class="text-muted">({{ $question->kko->bloom_level ?? 'C?' }})</small></td><td><button class="btn btn-sm btn-outline-info" data-bs-toggle="tooltip" title="{{ $question->indicator_text ?? 'Tidak ada indikator' }}"><i class="fas fa-eye"></i></button></td><td><div class="btn-group btn-group-sm"><a href="{{ route('questions.show',$question) }}" class="btn btn-outline-primary"><i class="fas fa-eye"></i></a><a href="{{ route('questions.edit',$question) }}" class="btn btn-outline-warning"><i class="fas fa-edit"></i></a><a href="{{ route('questions.duplicate',$question) }}" class="btn btn-outline-secondary"><i class="fas fa-copy"></i></a><button type="button" class="btn btn-outline-danger" data-confirm="Yakin hapus soal ini?" data-confirm-form="delete-form-{{ $question->id }}"><i class="fas fa-trash"></i></button><form id="delete-form-{{ $question->id }}" action="{{ route('questions.destroy',$question) }}" method="POST" class="d-none">@csrf @method('DELETE')</form></div></td></tr>@empty<tr><td colspan="8" class="text-center py-5">Belum ada soal.</td></tr>@endforelse</tbody></table></div><div class="d-flex justify-content-between align-items-center mt-3"><small class="text-muted">Menampilkan {{ $questions->firstItem() ?? 0 }}–{{ $questions->lastItem() ?? 0 }} dari {{ $questions->total() }} data</small>{{ $questions->links() }}</div></div>
<div class="modal fade" id="importModal" tabindex="-1"><div class="modal-dialog modal-lg"><div class="modal-content"><div class="modal-header"><h5 class="modal-title"><i class="fas fa-file-import me-2"></i> Import Soal</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><form action="{{ route('questions.import') }}" method="POST" enctype="multipart/form-data">@csrf<div class="modal-body"><div class="mb-3"><label class="form-label fw-bold">File Excel</label><input type="file" name="file" class="form-control" accept=".xlsx,.xls,.csv" required></div><div class="alert alert-info"><strong>Format level_kognitif:</strong> L1, L2, atau L3. KKO harus sesuai level yang dipilih.</div></div><div class="modal-footer"><button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Batal</button><button type="submit" class="btn btn-primary">Import</button></div></form></div></div></div>
@endsection
